# Next session — opening context (written 2026-06-04, overnight)

## Where we left off
Big U235 "cultural memory" push + an audit/hardening pass. RAG over emails +
invoices + documents is **live and realm-safe**; invoices are fully queryable with
amounts. Everything below is applied and verified unless marked otherwise.

## Done this session
- **Email browser page** upgraded (`/personal/emails`) — server-side filter/sort/
  search/paginate + detail (slugs `emails_browse/_accounts/_classifications/email_detail`). Shipped.
- **U235 cultural memory:**
  - V225 `home_ai.sanitise_full()` (no-trunc, strips style/script/injection) + `email_rag_chunks` (130,305 chunks, RLS).
  - V226 enriched invoice corpus (vendor + £ amounts → money questions answerable).
  - V227 `home_ai.cosine_sim()`, RLS on `search_vectors`, `email_rag_chunks.tsv`, and `v_research_corpus` now uses sanitised `email_chunk` (raw whole-email branch dropped = Rule-4 fix).
  - Embeddings: `email_chunk` 130,304 · `invoice_line` 21,720 · `document` 28 (nomic-embed-text, realm-tagged).
  - `/api/research/ask` (build-dashboard:8090, rebuilt) now realm-filters results + scales email_chunk (lexical-first). Invoice queries excellent; **email retrieval quality needs tuning** (common words like "account" flood the FTS pass — see below).
- **Capability registry**: `CAPABILITIES.md` (generated by `scripts/gen-capabilities.py`, wired into `/retro`, pointer in CLAUDE.md). CHECK IT before building.
- **Hardening**: backup cron restored (was lost) + fresh snapshot; security review in `.claude/decisions/2026-06-04-security-review.md`.

## Top focus next (in priority order)
1. **Services run as `postgres` superuser → RLS bypassed system-wide** (the root security finding). Migrate build-dashboard/google-fetch/bot-responder/playwright to `homeai_pipeline`/`homeai_readonly` per `scripts/u87-audit-superuser-usage.sh`. Deliberate, not blind — high breakage risk.
2. **Tune email RAG retrieval quality**: expand stopwords (account/statement/invoice/ltd…), prefer rarer terms, raise FTS candidate limit. Small build-dashboard change + rebuild. (Invoices already good.)
3. **Backup exits 3**: 4 root-owned scripts unreadable by `joly` cron — run backup as root or add restic `--exclude`.

## Open decisions awaiting Jo
- O2 (distilled cultural-memory store) defaulted to "structured extraction, not a knowledge graph" — confirm when we build Stage 4.
- Auto-load `CAPABILITIES.md` into every session (852 lines) vs grep-on-demand (current). Currently grep-on-demand.

## Fragile / watch
- build-dashboard was rebuilt + recreated with the Vault-harvested `POSTGRES_PASSWORD` (per its rebuild gotcha). Healthz 200.
- Uncommitted: V225-227, build-dashboard edit, frontend page, scripts, CAPABILITIES.md, sprint/decision docs. Entropy-scanned clean; not committed (Jo to decide).
