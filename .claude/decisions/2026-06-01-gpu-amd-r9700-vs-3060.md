# GPU upgrade — AMD Radeon AI PRO R9700 32 GB vs RTX 3060 (local AI)

**Status: OPEN / researching** — not yet decided. Pending (a) second opinions
from other AIs (prompt below), (b) a de-risking proof-of-concept before any buy.
**Date:** 2026-06-01. **Owner:** Jo. **Captured by:** Claude (this session).

## The decision
Whether to replace the **RTX 3060 12 GB (CUDA)** with the **Sapphire AMD Radeon
AI PRO R9700 32 GB GDDR6 (RDNA4, gfx1201)** as the box's single inference GPU —
to unlock a local 27–32B "medium" tier + local RAG over the email/invoice corpus,
and to **fine-tune small models to our narrow, repeatable ops** rather than rely
on general models that hit edge cases.

## Current state (verified 2026-06-01)
- RTX 3060 12 GB, driver 595.71.05. `ollama/ollama:0.22.1` (Docker, NVIDIA CDI
  passthrough `nvidia.com/gpu=all`, `OLLAMA_KEEP_ALIVE=-1`, `NUM_PARALLEL=2`).
- Models: `qwen2.5:7b` (4.7 GB), `phi4:14b` (9.1 GB), `nomic-embed-text`.
- 12 GB = one big model at a time (phi4 alone = 9 GB) → reload thrash.
- ai_usage shows local qwen doing ~the email-classify + invoice-ladder local tier;
  complex work escalates to Claude Haiku/Sonnet.

## R9700 analysis — the core trade
**The prize is 32 GB VRAM (2.7×) + ~640 GB/s (1.8× bandwidth). The risk is ROCm /
RDNA4 (gfx1201) software maturity on Linux — not the hardware.**

| | RTX 3060 (now) | R9700 (proposed) |
|---|---|---|
| VRAM | 12 GB | 32 GB |
| Bandwidth | ~360 GB/s | ~640 GB/s GDDR6 256-bit |
| Compute | CUDA (mature) | RDNA4 / ROCm gfx1201 (newer) |
| Power | ~170 W | ~300 W |

### What it unlocks
1. Local **medium tier** (qwen2.5:32b q4 ~20 GB) → most Haiku escalation in-house
   (privacy + headroom, not just cost — current cloud spend is already ~£0.69/30d).
2. No model thrash — hot + medium + embeddings all resident at once.
3. **Local RAG** over the ~75k backfilled emails (bodies done) + invoices.
4. Big local batch jobs (classify/summarise 5y archive) at zero cloud cost.

### Model recommendation (stay on qwen2.5 family — JSON/tool-use strength)
| Tier | Model | ~VRAM q4 | Job |
|---|---|---|---|
| Hot | qwen2.5:7b (or 14b) | 4.7 / 9 GB | classify, route, fast summaries |
| Medium (new local) | **qwen2.5:32b-instruct q4_K_M** | ~20 GB | invoice extraction, complex JSON, private-doc, reconciliation |
| Embeddings | nomic-embed-text | 0.3 GB | RAG |
| Cloud (kept) | Sonnet/Opus | — | highest-stakes reasoning only |

- **Not 70B** — needs ~40 GB at usable quant; won't fit. 27–32B is the sweet spot.
- Expected: ~15–25 tok/s on 32B, ~40–70 tok/s on 7B (bandwidth-bound). Fine for
  batch/async; acceptable not instant for live chat.

### How it slots into the stack
1. ROCm 6.4+ (`amdgpu-dkms`); confirm gfx1201 enumerated (maybe
   `HSA_OVERRIDE_GFX_VERSION` until native).
2. Compose: `ollama/ollama:rocm` + `/dev/kfd` + `/dev/dri` + `group_add:[video,render]`
   (replaces the NVIDIA CDI device). Retire `u226-gpu-recover.sh`; write an AMD equiv.
3. Re-tier the router (SPEC §7.3 / `static_context` thresholds) → escalate to
   local-32b before cloud. Update CLAUDE.md/AGENTS.md model table.
4. Upstream unchanged — Open WebUI / litellm / llm-router / n8n all hit
   `http://homeai-ollama:11434`. Only the GPU layer changes.

## Jo's thesis (2026-06-01)
Unsloth + optimisation should let us **fine-tune on the AMD card and overcome the
ROCm friction**, because our workflows are narrow and stable — tune to our ops
once, don't chase edge cases. (Sound instinct on the narrow-ops → fine-tune
angle; the open question is Unsloth-on-AMD specifically, which is historically
CUDA-first — verify the actual working AMD fine-tune path.)

## Verdict so far
- ✅ Capability/VRAM: clear, large upgrade; unlocks local-medium + RAG + fine-tune.
- ⚠️ Single biggest risk: RDNA4/ROCm + Ollama + (especially) **fine-tuning**
  toolchain maturity on Ubuntu 26.04 / kernel 7.0. The CUDA path "just works" today.
- 💡 Counterfactual: a used **RTX 3090 24 GB** (CUDA, ~£600–700) = less VRAM but
  zero ROCm risk and runs 32B q4 + LoRA fine-tune fine. R9700 wins on VRAM/
  bandwidth/power/warranty; 3090 wins on software certainty.

## Next steps (before any purchase)
1. Run the prompt below past 2–3 other AIs; collate answers here.
2. **De-risk POC** (smallest test that proves it): on a borrowed/returnable
   R9700 (or cloud RDNA4), prove BOTH (a) `ollama:rocm` inference and (b) one
   tiny QLoRA fine-tune + GGUF export + serve. Only buy if both pass.
3. If green: stage the compose/ROCm migration as a reversible change + router re-tier.

---

## Portable prompt for other AIs

```
I run a self-hosted AI stack for a small hospitality business (pub/inn/café) on a
Linux workstation, and I'm considering a GPU swap. I want a rigorous, current
(2025–2026) technical assessment — including where my plan is wrong.

CURRENT SETUP
- Host: AMD Threadripper 5945WX, 128 GB RAM, Ubuntu 26.04, kernel 7.0.
- GPU now: NVIDIA RTX 3060 12 GB (CUDA). Local inference via Ollama (Docker),
  models qwen2.5:7b + phi4:14b + nomic-embed-text. ~24 Docker services; Ollama
  is the only GPU consumer. Cloud (Claude Haiku/Sonnet) handles escalation today.
- The 12 GB VRAM is the constraint (one big model at a time; reload thrash).

PROPOSED GPU
- Sapphire AMD Radeon AI PRO R9700, 32 GB GDDR6, RDNA4 (gfx1201), ~4096 stream
  processors, ~640 GB/s, ~300 W. I'd remove the 3060 entirely (lose CUDA).

GOAL
- Run a 27–32B-class model locally (32 GB unlocks it) as a "medium" tier, plus
  embeddings for RAG over ~75k business emails + invoices.
- MORE IMPORTANTLY: FINE-TUNE small/medium open models to my SPECIFIC, NARROW,
  HIGHLY REPEATABLE workflows rather than relying on general models that keep
  hitting edge cases. My tasks are consistent and bounded:
    • email classification into fixed categories,
    • structured field extraction from invoices/receipts (vendor, date, net/vat/
      gross, line items) as strict JSON,
    • short hospitality reply drafting (reviews, guest emails),
    • reconciliation / routing decisions returning a fixed JSON "outcome" schema.
- Hypothesis: with Unsloth (or similar) + optimisation, I can fine-tune on the
  AMD card and overcome ROCm's rough edges on Linux, because my domain is narrow
  and stable — I'd tune to my ops once, not chase edge cases forever.

WHAT I NEED FROM YOU (be specific and honest, cite current state, flag risk):
1. ROCm reality on RDNA4 / gfx1201 (late 2025–2026): is inference (Ollama/
   llama.cpp) stable on this card+OS? Any HSA_OVERRIDE_GFX_VERSION or
   ROCm-version gotchas? Link real user reports if you can.
2. FINE-TUNING on AMD: does Unsloth actually support ROCm/RDNA4 today, and how
   well? If not, what DOES work on AMD right now — torchtune, axolotl, PEFT/LoRA
   on plain PyTorch-ROCm, llama.cpp finetune? Honest maturity assessment.
3. Given my narrow, repeatable tasks, is fine-tuning even the right tool vs
   (a) few-shot + JSON-schema-constrained decoding, (b) a small base model with
   good prompts? Where does QLoRA fine-tuning genuinely win for tasks like strict
   invoice-JSON extraction and fixed-category classification?
4. Recommended base model + method to fine-tune for these tasks on 32 GB
   (e.g. qwen2.5-7b/14b + QLoRA), and realistic VRAM/time on this card.
5. The full pipeline on AMD: dataset prep → LoRA train → merge → convert to GGUF
   → serve via Ollama. Where does this break on ROCm and how do people fix it?
6. Throughput expectations: rough tok/s for 7B and 32B inference on ~640 GB/s
   GDDR6, and whether that's adequate for batch classification + an async bot.
7. The honest counterfactual: would a used NVIDIA RTX 3090/4090 (CUDA, 24 GB) be
   a materially safer choice for fine-tuning specifically, and is the AMD 32 GB
   worth the ROCm risk for my use case?
8. A concrete, staged, reversible migration + a minimal proof-of-concept I should
   run FIRST to de-risk before committing (smallest test that proves ROCm
   inference + a LoRA fine-tune both work on this card).

Tell me clearly where my assumptions are over-optimistic, and what the single
biggest failure mode is. Assume I'm technical and can run Linux/Docker/ROCm.
```

## Other-AI responses (paste here as they come in)
- _(pending)_
