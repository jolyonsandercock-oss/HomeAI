# Frontend F1 — visibility gaps + integrity fixes (Opus-executable)

**Design decision (2026-07-03, Fable):** keep BOTH surfaces with a hard audience split —
Next.js `/app` = the owner surface (Jo's daily use); build-dashboard `:8090` = ops/build console.
No consolidation now (that is an F2 decision for Jo with usage data — which we start collecting
in this phase). Every F1 item lands in exactly one surface per that split.
Inventory evidence: frontend report 2026-07-02 (agent), cited file:line below.

## Deploy mechanics (executor: verify FIRST)
Establish how `homeai-frontend` ships: read its compose stanza + Dockerfile — if source is baked,
changes need an image rebuild (`docker compose build frontend && up -d frontend`; it publishes via
Caddy, no tailnet-bind race). Confirm `npm run build` works in-container or via the Dockerfile
before starting page work. Slugs: new read-slugs are added via the `query_whitelist` table
(see `lib/db.ts:118-140` — slug, sql, realm) — follow an existing slug row as the template
(SELECT one to copy conventions: naming, realm='work', LIMITs).

## Item 1 — `/app/ops` pipeline-health board (owner-visible R0 observability)
- New slugs (realm `work`): `ops_freshness` → `SELECT name, status, age_hours, sla_hours FROM ops.check_freshness() ORDER BY CASE status WHEN 'STALE' THEN 0 WHEN 'NO_DATA' THEN 1 ELSE 2 END, age_hours DESC NULLS FIRST LIMIT 80`;
  `ops_alerts` → firing system_alerts (alertname, severity, starts_at, acknowledged) LIMIT 20;
  `ops_recent_failures` → last 24h `ops.pipeline_runs` failed rows LIMIT 20.
- New page `app/ops/page.tsx` modeled on `app/cafe/page.tsx` (small, clean): three sections —
  traffic-light freshness grid (green ok / amber NO_DATA / red STALE), firing alerts list,
  recent failures. Add to `components/shell/nav.ts` (work realm, not a mobile tab).
- Acceptance: page renders live data; STALE items visibly red; zero writes.

## Item 2 — `/app/reviews` tracker (closes bot_instructions #1161, pending since 06-25)
- Slugs: `reviews_recent` → guest_reviews last 50 (source, rating, reviewer, text excerpt,
  review_date); `reviews_source_health` → per-source count + max(review_date) + the
  review_listings scrape status (google currently `unparsed`, tripadvisor `fetch_fail` — surfacing
  WHY sources are missing is the point); `reviews_blend` → the same blended-rating calc the U39
  strip uses (read `main.py:4312` /api/reviews/queue for the blend logic; Expedia /10 normalised
  per commit ce247be).
- Page `app/reviews/page.tsx`: blend headline, per-source health row (with dead-scraper warnings),
  recent reviews list. Nav entry + mobile tab (Jo checks reviews on phone).
- Mark #1161: `UPDATE bot_instructions SET status='done', triage_summary='reviews tracker shipped /app/reviews 2026-07-03 + aggregator health surfaced; scraper revival queued separately' WHERE id=1161;`
- Acceptance: page shows all 4 sources incl. the 2 Expedia rows; dead scrapers flagged amber.

## Item 3 — link/integrity fixes (both surfaces, one commit)
- `services/build-dashboard/static/counterparty-review.html:29` + `landing.html`: `/dashboard/` → `/` (relative to :8090 root). NOTE main.py/static are BAKED (memory: image rebuild required) — rebuild build-dashboard per `feedback_dashboard_image_rebuild` memory (use scripts/recreate-with-secrets.sh after `docker compose build build-dashboard`).
- `app/admin/invoices/[id]/page.tsx:130,136`: hardcoded `http://100.104.82.53:8090/...` → relative FQDN path (verify which Caddy route serves those viewers).
- `git rm app/sales/page.tsx.bak` (tracked 28KB dead twin, under services/homeai-frontend/).
- Remove the permanently-empty drift widget from `static/index.html` (`/api/drift/current` returns empty always — verify once, then delete the widget block; leave the API).
- Acceptance: no `/dashboard/` string in either static page; no hardcoded tailnet IP in app/.

## Item 4 — counterparty review reachable (small)
- Add nav entry in `/app` linking to the (now-fixed) counterparty-review page via the FQDN route —
  full port to Next.js is F2. 42 items are pending review; Jo needs a working door.

## Item 5 — telemetry + realm hardening (one commit)
- Caddyfile: add a `log` directive (JSON, rotating) — usage data feeds the F2 consolidation decision.
- Caddy: strip client-supplied `Remote-Groups`/`Remote-User` headers on ALL routes before
  forward_auth re-injects them (the documented spoofing follow-up, `lib/realm.ts:28-29`).
  CAUTION: Caddyfile has uncommitted live drift — isolate hunks, verify staged diff contains only
  your changes. Reload Caddy (config is bind-mounted — verify, then `docker exec homeai-caddy
  caddy reload --config /etc/caddy/Caddyfile`).
- Acceptance: a forged `Remote-Groups: admins` from outside Authelia gets `work`-realm defaults,
  not elevated; Caddy access log populating.

## Out of scope (F2, needs Jo + usage data)
Two-stack consolidation; review-scraper revival (google unparsed / tripadvisor fetch_fail);
mobile polish; the 41 hardcoded `SET app.current_entity='1'` in main.py (R3-adjacent);
per-route OIDC tiers.
