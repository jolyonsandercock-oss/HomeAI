# Tasks remaining for Phase 1 close-out.
# Edit freely — re-read on every /api/snapshot.

hands_off:
  - id: column-order-cleanup
    title: Fix accommodation/epos/till column order drift
    detail: Schema drift detector flags 56 lines of cosmetic column-order differences (entity_id placed AFTER created_at in live, BEFORE in init-db.sql). Either rewrite init-db to match live, or accept as benign and add an exclusion to the drift detector.
    estimate: 30 min

  - id: caddy-routes
    title: Caddy reverse-proxy routes for new services
    detail: Build dashboard at /dashboard, MarkItDown internal, Authelia at /auth/. Currently only Tailscale-IP:port direct access.
    estimate: 30 min

  - id: ci-autofix
    title: CI Auto-Fix sketch (Phase 2)
    detail: GitHub Actions workflow that runs RLS test suite, sanitiser fixture, P2 fixture, schema drift on PRs. Needs the repo first (Bucket 3 — needs you).
    estimate: 45 min once repo exists

needs_user:
  # All three remaining user-blocked items are scoped together as U29
  # (Phase 1 close-out). See /home_ai/.claude/sprints/U29-phase1-closeout.md.
  - id: xero-oauth
    title: P3 Xero OAuth + P2 matching unlock (U29 chunk 1)
    detail: Register at developer.xero.com, run OAuth Playground for refresh tokens (one for Trading, one for Estates), store at secret/xero/{trading,estates}. After tokens land, run scripts/u29-xero-creds.sh (will be written this sprint).
    estimate: 30 min

  # ✓ scraper-creds completed 2026-05-11 — TouchOffice + Caterbook creds in Vault.
  # ✓ TouchOffice scraper + ingest live; daily 03:00 cron installed. Backfill
  #   in progress (30 days first, then 3 years). See /touchoffice in dashboard.
  # → Caterbook pivoted from browser scrape to email-driven pipeline.
  #   Plan in .claude/sprints/U28-caterbook-email-pipeline.md.

  - id: gsheets-oauth
    title: P7 Cashing Up — Google Sheets OAuth
    detail: Create the cashing-up sheet (columns A–J per Appendix C), then OAuth flow for Sheets API.
    estimate: 15 min

  - id: telegram-smtp
    title: P10 Daily Digest — Telegram bot + SMTP
    detail: BotFather → bot token + chat_id → Vault. SMTP creds (Gmail app password works) → Vault.
    estimate: 20 min

  # ▸ nas-mount POSTPONED 2026-05-11 (user decision). GitHub off-host-backup
  #   handles config-tree DR (U26 option 9). Re-add when NAS hardware is on hand.
