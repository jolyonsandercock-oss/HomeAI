# Tasks remaining for Phase 1 close-out.
# Edit freely — re-read on every /api/snapshot.

hands_off:
  - id: column-order-cleanup
    title: Fix accommodation/epos/till column order drift
    detail: Schema drift detector flags 56 lines of cosmetic column-order differences (entity_id placed AFTER created_at in live, BEFORE in init-db.sql). Either rewrite init-db to match live, or accept as benign and add an exclusion to the drift detector.
    estimate: 30 min

  - id: caddy-routes
    title: Caddy reverse-proxy routes for new services
    detail: Build dashboard at /dashboard, MarkItDown internal, Authelia at /auth/. Currently only Tailscale-IP:port direct access.
    estimate: 30 min

  - id: ci-autofix
    title: CI Auto-Fix sketch (Phase 2)
    detail: GitHub Actions workflow that runs RLS test suite, sanitiser fixture, P2 fixture, schema drift on PRs. Needs the repo first (Bucket 3 — needs you).
    estimate: 45 min once repo exists

needs_user:
  - id: xero-oauth
    title: P3 Xero OAuth + P2 matching unlock
    detail: Register at developer.xero.com, run OAuth Playground for refresh tokens (one for Trading, one for Estates), store at secret/xero/{trading,estates}.
    estimate: 30 min

  - id: gmail-routing
    title: Decide which Gmail account TouchOffice and Caterbook send to
    detail: P5/P6 need a single decision — personal1, personal2, or workspace. Then I wire the email routing rules.
    estimate: 1 min decision

  - id: gsheets-oauth
    title: P7 Cashing Up — Google Sheets OAuth
    detail: Create the cashing-up sheet (columns A–J per Appendix C), then OAuth flow for Sheets API.
    estimate: 15 min

  - id: telegram-smtp
    title: P10 Daily Digest — Telegram bot + SMTP
    detail: BotFather → bot token + chat_id → Vault. SMTP creds (Gmail app password works) → Vault.
    estimate: 20 min

  - id: nas-mount
    title: Mount NAS at /mnt/mycloud
    detail: CIFS or NFS mount of WD MyCloud. After mounting, I'll repoint Restic via RESTIC_REPO env override.
    estimate: 10 min
